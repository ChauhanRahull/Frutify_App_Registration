import React from "react";
import { View, Text } from "react-native";

const HomeIcon = () => {
    return (
        <View>
            <Text style={{ fontSize: 24, fontWeight: "bold" }}>🛒 Groceries</Text>
        </View>
    );
};

export default HomeIcon;
